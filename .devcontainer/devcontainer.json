{
    "build": {
        "context": ".",
        "dockerfile": "./Dockerfile"
    },
    "containerUser": "developer",
    "customizations": {
        "vscode": {
            "settings": {
                "editor.renderWhitespace": "all",
                "files.insertFinalNewline": true,
                "files.trimTrailingWhitespace": true
            }
        }
    },
    "features": {
        "ghcr.io/devcontainers/features/github-cli:1": {},
        "ghcr.io/devcontainers/features/sshd:1": {}
    },
    "hostRequirements": {
        "cpus": 2,
        "memory": "4gb",
        "storage": "16gb"
    },
    "mounts": [
        "source=lv,target=/persist,type=volume",
        "source=lv,target=/sandboxes,type=volume"
    ],
    "postCreateCommand": {
        "Own Volumes": "sudo chown -R $(whoami): /persist /sandboxes",
        "Run Final Setup Script": "/sandboxes/devtools/.devcontainer/final.sh"
    },
    "remoteUser": "developer",
    "runArgs": [ // For C++ Debuggers
        "--cap-add=SYS_PTRACE",
        "--security-opt",
        "seccomp=unconfined"
    ],
    "workspaceFolder": "/sandboxes",
    "workspaceMount": "source=${localWorkspaceFolder},target=/sandboxes/devtools,type=bind,consistency=cached"
}
